# Build-time Python is selected with the PYO3_PYTHON environment variable.
# Do not pin a machine-specific interpreter here. See README.md for macOS/Linux
# setup and use `beater doctor` to inspect the embedded interpreter + venv.
